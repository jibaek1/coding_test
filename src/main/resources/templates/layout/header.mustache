<header class="site-header">
    <div class="container">
        <div class="brand">
            <a href="/">tenco</a>
        </div>
        <nav class="nav-links">
            {{#sessionUser}}
                <a href="/update-form">회원정보</a>
                <a href="/logout">로그아웃</a>
            {{/sessionUser}}
            {{^sessionUser}}
                <a href="/login-form">로그인</a>
                <a href="/join-form">회원가입</a>
            {{/sessionUser}}
        </nav>
    </div>
</header>

<style>
    .site-header { background:#fff; border-bottom:1px solid #eee; }
    .site-header .container { max-width:960px; margin:0 auto; padding:12px 16px; display:flex; justify-content:space-between; align-items:center;}
    .site-header .brand a { font-size:22px; font-weight:bold; color:#333; text-decoration:none;}
    .site-header .nav-links a { margin-left:16px; color:#333; text-decoration:none; font-size:15px;}
    .site-header .nav-links a:hover { color:#007bff; }
</style>
